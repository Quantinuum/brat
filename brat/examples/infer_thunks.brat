ext "arith.i2f" to_float(i :: Int) -> Float

id(X :: *, X) -> X
id(_, x) = x

map(X :: *, Y :: *, { X -> Y }, List(X)) -> List(Y)
map(_, _, _, []) = []
map(X, Y, f, x ,- xs) = f(x) ,- map(X, Y, f, xs)

--!exec [[5.0]]
test :: List(Float)
test = map(!, !, {f => f(5)}, [to_float])
